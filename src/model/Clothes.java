package model;

public class Clothes extends Product{
    public Clothes(long id, String name, Double weight, Float size) {
        super(id, name, weight, size);
    }// Унаследовали model.Product.
}
